# DropDPI — Прокси для обхода DPI

DropDPI — это система проксирования трафика, разработанная для обхода систем глубокого анализа пакетов (DPI). Она работает, разбивая данные на мелкие зашифрованные части (чанки) и отправляя их через несколько параллельных TCP-соединений.

## 🚀 Как это работает?

Обычный прокси отправляет данные одним потоком. DPI системы могут заглянуть внутрь пакета или проанализировать его размер и тайминги, чтобы понять, что вы передаете (например, VPN протокол или HTTPS запрос к запрещенному ресурсу).

**DropDPI делает следующее:**

1.  **Дробит всё**: Весь поток данных (и заголовки, и **тело запроса**, и файлы) разбивается на мелкие кусочки (по 1024 байта).
2.  **Шифрует**: Каждый кусочек шифруется алгоритмом AES-256-GCM. Для внешнего наблюдателя это выглядит как случайный шум.
3.  **Перемешивает**: Кусочки отправляются в случайном порядке через несколько (по умолчанию 3) параллельных соединений.
4.  **Собирает**: На другой стороне сервер собирает всё обратно в правильном порядке.

**Ответ на ваш вопрос:** Да, система дробит **абсолютно всё**. Для неё нет разницы между заголовком HTTP и телом запроса (картинкой или JSON). Это просто поток байт, который нарезается в фарш, шифруется и отправляется.

---

## 🛠 Установка и настройка

Вам понадобится:

1.  **Сервер (VPS)** за пределами блокировок (Linux).
2.  **Клиент (Ваш ПК)** (Windows, macOS или Linux).

### Часть 1: Настройка Сервера (VPS)

Зайдите на ваш сервер по SSH.

#### 1. Установите Go (если нет)

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install golang -y
```

#### 2. Скачайте и соберите проект

Загрузите код на сервер (или создайте файлы вручную). Затем соберите серверную часть:

```bash
# Переходим в папку проекта
cd dropdpi

# Собираем бинарный файл сервера
go build -o dropdpi-server cmd/server/main.go
```

#### 3. Запуск сервера

Простой запуск:

```bash
./dropdpi-server
```

Сервер запустится на порту `8443`. Убедитесь, что этот порт открыт в фаерволе.

#### 4. (Рекомендуется) Автозапуск через Systemd

Чтобы сервер работал всегда и перезапускался при сбоях:

Создайте файл `/etc/systemd/system/dropdpi.service`:

```ini
[Unit]
Description=DropDPI Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/dropdpi
ExecStart=/root/dropdpi/dropdpi-server
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите службу:

```bash
sudo systemctl daemon-reload
sudo systemctl enable dropdpi
sudo systemctl start dropdpi
```

---

### Часть 2: Настройка Клиента (Ваш ПК)

На вашем локальном компьютере.

#### 1. Настройка адреса сервера

Перед сборкой откройте файл `cmd/client/main.go` и укажите IP-адрес вашего сервера:

```go
var (
    // Замените 127.0.0.1 на IP вашего VPS
    serverAddr = "ВАШ_IP_СЕРВЕРА:8443"

    // Ключ шифрования должен совпадать на клиенте и сервере!
    serverKey  = []byte("0123456789abcdef0123456789abcdef")
)
```

#### 2. Сборка и запуск

```bash
# Собираем клиент
go build -o dropdpi-client cmd/client/main.go

# Запускаем
./dropdpi-client
```

Клиент запустится и будет слушать порт `127.0.0.1:1080` (SOCKS5).

---

### Часть 3: Настройка Браузера и Программ

Теперь, когда клиент запущен, нужно направить трафик через него.

#### 🦊 Firefox / Chrome (через расширения)

Рекомендуется использовать расширение **FoxyProxy** или **SwitchyOmega**.

1.  Установите расширение.
2.  Создайте новый профиль прокси:
    - **Protocol**: SOCKS5
    - **Server**: 127.0.0.1
    - **Port**: 1080
3.  Активируйте этот профиль.

#### ✈️ Telegram

1.  Настройки -> Данные и память -> Использовать прокси.
2.  Добавить прокси:
    - **SOCKS5**
    - **Сервер**: 127.0.0.1
    - **Порт**: 1080

#### 🖥 Терминал (curl, wget)

```bash
# Использовать для одной команды
curl -x socks5h://127.0.0.1:1080 https://google.com

# Установить для всей сессии
export all_proxy=socks5://127.0.0.1:1080
export http_proxy=socks5://127.0.0.1:1080
export https_proxy=socks5://127.0.0.1:1080
```

---

## 🔧 Решение проблем

**1. "bind: address already in use"**
Это значит, что порт занят. Возможно, вы уже запустили программу.

- На Linux/Mac: `pkill -f dropdpi`
- Затем запустите снова.

**2. Данные не идут**

- Проверьте, что ключи (`serverKey`) в `cmd/server/main.go` и `cmd/client/main.go` **абсолютно одинаковые**.
- Проверьте фаервол на сервере (ufw/iptables), разрешен ли порт 8443.

**3. Низкая скорость**
Система дробит данные и шифрует их, это создает нагрузку на процессор. Для видео 4K может не подойти, но для веб-серфинга должно быть достаточно.
